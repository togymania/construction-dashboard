# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Validation & Settings
pydantic==2.10.3
pydantic-settings==2.6.1
# email-validator is required by pydantic's EmailStr; without it,
# importing any schema that uses EmailStr fails at app startup.
email-validator==2.2.0

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# Security
python-jose[cryptography]==3.3.0
# bcrypt>=4.0 raises on >72-byte passwords (instead of truncating),
# which crashes passlib 1.7.4's backend-detection at startup. Pinning
# bcrypt<4 keeps passlib's hash/verify path working.
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.19

# HTTP & Utils
httpx==0.28.1
python-dotenv==1.0.1

# Excel import
openpyxl==3.1.5
rapidfuzz==3.10.1

# PDF + LLM (Day 11+)
# anthropic 0.39 used the old httpx<0.28 API (proxies kwarg). Modern
# httpx removed it, so we follow the newer SDK that handles both.
pdfplumber==0.11.4
anthropic>=0.40.0

# Dev & Testing
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.3
mypy==1.13.0
